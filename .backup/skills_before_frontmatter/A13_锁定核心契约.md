---
name: 锁定核心契约
description: 将 AGENTS.md 的第2-5章标记为已锁定，禁止后续修改。
allowed-tools: Read, Write
---

# 前置条件
- `.ai/进度状态.json` 中的 `current_step` 必须为 `A13`。
- `A12` 已完成。

## 目标
将 `AGENTS.md` 中第2-5章标记为"已锁定"，禁止后续修改（除非用户明确要求变更）。

## Action
- 读取 `AGENTS.md`。
- 在第2章、第3章、第4章、第5章的末尾分别添加 `<!-- locked -->` 注释。
- 更新 `.ai/进度状态.json` 中的 `locked_chapters` 数组，记录已锁定的章节（如 `["2","3","4","5"]`）。
- 保存文件。

## Assert
- 注释已添加。
- 进度状态中记录了锁定信息。

## Artefact
- 更新后的 `AGENTS.md`（带锁定标记）。
- 更新后的进度状态。

## Analysis
- 检查是否有其他章节被意外锁定。

## Adjust
- 若用户后续要求修改锁定章节，需通过变更流程（阶段E）。

## Advance
- 更新 `.ai/进度状态.json`：
  ```json
  {
    "current_step": "A14",
    "steps_completed": ["...", "A13"]
  }
  ```
- 提示用户：下一步应执行 A14。

## 干预
无。
