---
name: 更新核心契约第8章
description: 将最终部署的服务器地址、域名、SSL 状态等信息写入核心契约 AGENTS.md 的第8章。
allowed-tools: Read, Write
---

# 前置条件
- `.ai/进度状态.json` 中的 `current_step` 必须为 `D11`。
- `D10` 已完成（健康检查通过）。

## 目标
将最终部署的服务器地址、域名、SSL 状态等信息写入核心契约 `AGENTS.md` 的第8章，作为项目的部署配置记录。

## Action
- 读取 `.ai/config/deployment.yaml` 中的部署配置。
- 读取 `AGENTS.md` 当前内容。
- 更新第8章"部署配置"：
  - 服务器地址：从 deployment.yaml 中获取 `server`。
  - 域名：从 deployment.yaml 中获取 `domain`。
  - SSL：从 deployment.yaml 中获取 `ssl`（true/false）。
  - 部署时间：记录当前时间戳。
- 保存更新后的 `AGENTS.md`。

## Assert
- 第8章内容已填充，无占位符残留。
- 信息与实际部署一致。

## Artefact
- 更新后的 `AGENTS.md`。

## Analysis
- 检查是否有其他章节引用了部署信息，保持一致性。

## Adjust
- 若 deployment.yaml 缺失某些字段，提示用户补充或使用默认值。

## Advance
- 更新 `.ai/进度状态.json`：
  ```json
  {
    "current_step": "D12",
    "steps_completed": ["...", "D11"]
  }
  ```
- 提示用户：下一步应执行 D12。

## 干预
无。
