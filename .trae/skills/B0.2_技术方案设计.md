---
name: 技术方案设计
description: 基于功能需求澄清的结果，设计详细的技术实现方案，包括API设计、数据库变更、核心逻辑等。
allowed-tools: Read, Write, Bash
---

# 前置条件
- `.ai/进度状态.json` 中的 `current_step` 必须为 `B0.2`。
- `B0.1` 已完成（功能需求澄清已输出）。

## 目标
根据功能需求文档，设计详细的技术方案，包括API接口、数据库表结构、核心算法、外部依赖等，为后续任务规划和编码提供依据。

## Action
- 读取功能需求文档（`specs/features/{功能名}/需求.md`）和数据模型（`data-model.json`）。
- 设计API接口：
  - 确定请求方法、路径、参数、响应格式。
  - 定义错误码和错误处理逻辑。
- 设计数据库变更：
  - 新增/修改表、字段、索引。
  - 编写迁移脚本（如 SQL 或 ORM 迁移）。
- 设计核心业务逻辑：
  - 伪代码或流程图。
  - 关键算法说明。
- 列出所需的外部依赖或第三方服务。
- 生成技术方案文档（`specs/features/{功能名}/技术方案.md`），遵循项目模板。

## Assert
- 技术方案覆盖所有验收标准。
- API 接口与数据模型一致（字段类型、约束）。
- 数据库变更与数据模型同步。

## Artefact
- 技术方案文档（`.md` 格式）。

## Analysis
- 评估方案的性能、安全性、可维护性。
- 识别潜在的技术风险（如第三方服务不可用、数据量增长等），提出缓解措施。

## Adjust
- 如果设计方案中发现需求不明确，返回 B0.1 补充澄清。
- 若存在多种技术选型，列出对比并给出推荐。

## Advance
- 更新 `.ai/进度状态.json`：
  ```json
  {
    "current_step": "B1",
    "steps_completed": ["...", "B0.2"]
  }
  ```
- 提示用户：下一步应执行 B1（分析字段语义）。

## 干预
无（自动生成，用户可后续审核修改）。
