---
name: 更新核心契约
description: 将技术栈、数据边界引用、页面需求等写入 AGENTS.md 的第2-5章。
allowed-tools: Read, Write
---

# 前置条件
- `.ai/进度状态.json` 中的 `current_step` 必须为 `A12`。
- `A11` 已完成。

## 目标
将技术栈、数据边界引用、页面需求等动态配置写入核心契约 `AGENTS.md` 的第2-5章。

## Action
- 读取 `AGENTS.md` 模板（若不存在则从 `.ai/tmp/AGENTS.md.template` 复制）。
- 替换占位符：
  - 第2章：技术栈（从进度状态读取，若无则使用默认值 `前端: React, 后端: FastAPI, 数据库: PostgreSQL`）。
  - 第3章：写入 `data-model.json` 的引用路径（`详见 data-model.json`）。
  - 第4章：写入 `详见 .ai/config/page-requirements.yaml 的 input_pages`。
  - 第5章：写入 `详见 .ai/config/page-requirements.yaml 的 output_pages`。
- 保存更新后的 `AGENTS.md`。

## Assert
- 所有占位符都被替换，没有残留 `{待填充}`。
- 文件格式为 Markdown，章节编号正确。

## Artefact
- 更新后的 `AGENTS.md`。

## Analysis
- 检查技术栈是否合理（如前后端组合是否常见）。
- 检查引用路径是否存在（如 `data-model.json` 已生成）。

## Adjust
- 如果技术栈缺失，提示用户选择（可在 A1 阶段补充，但当前使用默认值）。
- 若 `page-requirements.yaml` 尚未生成，暂时留空引用，后续步骤会创建。

## Advance
- 更新 `.ai/进度状态.json`：
  ```json
  {
    "current_step": "A13",
    "steps_completed": ["...", "A12"]
  }
  ```
- 提示用户：下一步应执行 A13。

## 干预
无（自动填充）。


## When to Use This Skill
Use this skill when A12_更新核心契约 is required, such as during phase A tasks.

## Not For
- This skill does not handle other phases or unrelated tasks.
