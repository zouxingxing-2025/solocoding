---
name: 运行沙箱测试
description: 在沙箱环境中运行所有测试用例，收集测试结果和覆盖率。
allowed-tools: Bash, Read
---

# 前置条件
- `.ai/进度状态.json` 中的 `current_step` 必须为 `C3`。
- `C2` 已完成。

## 目标
在沙箱环境中运行所有测试用例（单元测试、集成测试），收集测试结果（通过/失败）和代码覆盖率。

## Action
- 进入沙箱环境（复用 C1 的环境）。
- 执行测试命令：
  - Python: `pytest tests/ --cov=src --cov-report=html`
  - Node.js: `npm test -- --coverage`
- 捕获输出，记录总用例数、通过数、失败数、错误数、跳过数。
- 生成测试报告（`.ai/reports/test_report.html`）和覆盖率报告。
- 若测试失败，将失败用例的详细信息记录到 `.ai/reports/failed_tests.log`。

## Assert
- 测试执行完成，无异常中断。
- 核心功能模块的测试通过率不低于 90%（可配置）。

## Artefact
- 测试报告（`.ai/reports/test_report.html`）
- 失败日志（`.ai/reports/failed_tests.log`）

## Analysis
- 分析失败原因：断言错误、环境问题、代码 bug。
- 若覆盖率低于阈值，标记需要补充测试的模块。

## Adjust
- 如果测试失败且原因是环境问题，自动修复环境后重试。
- 若失败用例过多（>10%），提示用户优先修复。

## Advance
- 更新 `.ai/进度状态.json`：
  ```json
  {
    "current_step": "C4",
    "steps_completed": ["...", "C3"]
  }
  ```
- 提示用户：下一步应执行 C4。

## 干预
无。


## When to Use This Skill
Use this skill when C3_运行沙箱测试 is required, such as during phase C tasks.

## Not For
- This skill does not handle other phases or unrelated tasks.
